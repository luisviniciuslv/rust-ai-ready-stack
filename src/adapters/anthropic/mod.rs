/// Exemplo de adapter para Claude (Anthropic).
///
/// TODO: implementar integração real (requests, auth, mapeamento para ChatProviderResponse).
#[allow(dead_code)]
pub struct AnthropicChatProvider;
